const ErrorDisplay = () => {
  return (
    <div style={{ display: 'flex', justifyContent: 'center' }}>
      <p>It seems an error has occured, try again at a later time...</p>
    </div>
  );
};

export default ErrorDisplay;
